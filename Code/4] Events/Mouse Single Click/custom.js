"use strict";

$( document ).ready( function( )
{
	$( "div" ).click( function( )
	{
		console.log( "A div is clicked" );
	} );

	$( "#EpicID" ).click( function( )
	{
		alert( "Epic id alert" );
	} );
} );

